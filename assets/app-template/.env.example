# === Anthropic Configuration (default provider) ===
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# === Databricks Configuration ===
# RECOMMENDED: Use Databricks CLI for authentication
# 1. Run: databricks configure --token
# 2. Enter your workspace URL and token when prompted
# 3. This creates ~/.databrickscfg with your credentials
# 4. The agent will automatically use these credentials!

# Optional: Specify which CLI profile to use (if you have multiple)
# DATABRICKS_PROFILE=DEFAULT           # Use DEFAULT profile (default behavior)
# DATABRICKS_PROFILE=staging           # Use staging profile
# DATABRICKS_PROFILE=production        # Use production profile

# Optional: Override workspace URL (if not using CLI profile)
# DATABRICKS_HOST=https://your-workspace.databricks.com

# Alternative: Set credentials directly (NOT RECOMMENDED - use CLI instead)
# DATABRICKS_TOKEN=your-databricks-token-here

# Production: Use Service Principal authentication
# DATABRICKS_CLIENT_ID=your-client-id
# DATABRICKS_CLIENT_SECRET=your-client-secret

# === OpenAI Configuration ===
OPENAI_API_KEY=your-openai-api-key-here

# === Azure OpenAI Configuration ===
AZURE_OPENAI_API_KEY=your-azure-api-key-here
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com

# === Agent Configuration (optional) ===
# Override defaults via environment variables
AGENT_PROVIDER=databricks                         # databricks, anthropic, openai, azure (default: databricks)
AGENT_MODEL=databricks-claude-sonnet-4-5         # Provider-specific model name (default: databricks-claude-sonnet-4-5)
AGENT_WORKSPACE=/tmp/agent-workspace             # Workspace directory (default: /tmp/agent-workspace)
AGENT_AUTO_APPROVE=true                           # Auto-approve agent actions (MUST be true for API, default: true)
AGENT_ENDPOINT=                                   # Custom endpoint (Databricks, Azure)
